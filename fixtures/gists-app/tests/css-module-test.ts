// import type { Browser, Page } from "puppeteer";
// import puppeteer from "puppeteer";
// import * as Utils from "./utils";

// const testPort = 3000;
// const testServer = `http://localhost:${testPort}`;

export {};

describe("CSS module import", () => {
  //   let browser: Browser;
  //   let page: Page;
  //   beforeEach(async () => {
  //     browser = await puppeteer.launch();
  //     page = await browser.newPage();
  //   });

  //   afterEach(() => browser.close());

  it.todo("uses the hashed classname", async () => {
    // await page.goto(`${testServer}/one`);
    // await Utils.reactIsHydrated(page);
    // await page.waitForSelector('[data-test-id="counter-button"]');
    // TODO: Get the classname and ensure it has a hash
  });
});
